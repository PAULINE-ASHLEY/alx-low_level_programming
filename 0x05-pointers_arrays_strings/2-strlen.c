#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: displays a string
 *
 * Return: Always 0
 */
int _strlen(char *s)
{ 
	int i;

	i = 0;

	while (s[i] != '\0')
	{
		i++;
	}

	return (1);
}
