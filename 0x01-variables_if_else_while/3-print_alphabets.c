#include <stdio.h>

/**
 * main - End point
 *
 * Return:Always 0 (success)
 */
int main(void)
{
	int ch;
	int b;

	for (ch = 'a' ch <= 'z' ch++)
		putchar(ch);

	for (b = 'A' b <= 'Z' b++)
		putchar(b);

putchar('\n');

	return (0);
}
